[tools]
helm = "3.21.3"

[tasks."validate:all"]
description = "Run all Argo app wiring and render validations"
run = '''
mise run validate:cert-manager
mise run validate:cert-manager-config
mise run validate:gateway-api-crds
mise run validate:gateway-api-config
mise run validate:k8s-playground-service
mise run validate:istio-base
mise run validate:istiod
mise run validate:istio-cni
mise run validate:istio-ingressgateway
'''

[tasks."validate:cert-manager"]
description = "Validate cert-manager Application wiring and external values render"
usage = '''
flag "--platform-config <platform-config>" help="local platform-config repo path" default="../k8s-playground-platform-config"
'''
run = '''
app="clusters/kind/apps/cert-manager.yaml"
repo_url="https://github.com/mblayman/k8s-playground-platform-config.git"
target_revision="main"
values_path="platform/cert-manager/values.yaml"
application="$(<"$app")"

case "$application" in
  *"repoURL: $repo_url"*) ;;
  *) echo "$app does not point values at $repo_url" >&2; exit 1 ;;
esac

case "$application" in
  *"targetRevision: $target_revision"*) ;;
  *) echo "$app does not use targetRevision $target_revision for values" >&2; exit 1 ;;
esac

case "$application" in
  *"- \$values/$values_path"*) ;;
  *) echo "$app does not use value file \$values/$values_path" >&2; exit 1 ;;
esac

helm template cert-manager cert-manager \
  --repo https://charts.jetstack.io \
  --version v1.21.0 \
  --namespace cert-manager \
  --include-crds \
  --values "{{ usage.platform_config }}/$values_path" \
  >/dev/null
'''

[tasks."validate:cert-manager-config"]
description = "Validate cert-manager-config Application wiring and external source render"
usage = '''
flag "--platform-config <platform-config>" help="local platform-config repo path" default="../k8s-playground-platform-config"
'''
run = '''
app="clusters/kind/apps/cert-manager-config.yaml"
repo_url="https://github.com/mblayman/k8s-playground-platform-config.git"
target_revision="main"
source_path="platform/cert-manager-config"
application="$(<"$app")"

case "$application" in
  *"repoURL: $repo_url"*) ;;
  *) echo "$app does not point at $repo_url" >&2; exit 1 ;;
esac

case "$application" in
  *"targetRevision: $target_revision"*) ;;
  *) echo "$app does not use targetRevision $target_revision" >&2; exit 1 ;;
esac

case "$application" in
  *"path: $source_path"*) ;;
  *) echo "$app does not point at $source_path" >&2; exit 1 ;;
esac

kubectl kustomize "{{ usage.platform_config }}/$source_path" >/dev/null
'''

[tasks."validate:gateway-api-crds"]
description = "Render the Gateway API standard CRDs"
run = 'kubectl kustomize "https://github.com/kubernetes-sigs/gateway-api/config/crd?ref=v1.6.0" >/dev/null'

[tasks."validate:gateway-api-config"]
description = "Render the platform Gateway API config"
run = "kubectl kustomize components/platform/gateway-api-config >/dev/null"

[tasks."validate:k8s-playground-service"]
description = "Render the k8s-playground-service app manifests"
run = "kubectl kustomize components/apps/k8s-playground-service >/dev/null"

[tasks."validate:istio-base"]
description = "Render the Istio base Helm chart with local values"
run = '''
helm template istio-base base \
  --repo https://istio-release.storage.googleapis.com/charts \
  --version 1.30.2 \
  --namespace istio-system \
  --values components/platform/istio/base/values.yaml \
  >/dev/null
'''

[tasks."validate:istiod"]
description = "Render the istiod Helm chart with local values"
run = '''
helm template istiod istiod \
  --repo https://istio-release.storage.googleapis.com/charts \
  --version 1.30.2 \
  --namespace istio-system \
  --values components/platform/istio/istiod/values.yaml \
  >/dev/null
'''

[tasks."validate:istio-cni"]
description = "Render the Istio CNI Helm chart with local values"
run = '''
helm template istio-cni cni \
  --repo https://istio-release.storage.googleapis.com/charts \
  --version 1.30.2 \
  --namespace istio-system \
  --values components/platform/istio/cni/values.yaml \
  >/dev/null
'''

[tasks."validate:istio-ingressgateway"]
description = "Render the Istio ingress gateway Helm chart with local values"
run = '''
helm template istio-ingressgateway gateway \
  --repo https://istio-release.storage.googleapis.com/charts \
  --version 1.30.2 \
  --namespace istio-system \
  --values components/platform/istio/ingressgateway/values.yaml \
  >/dev/null
'''
